



// User Inferface Logic

function handleSubmission(event){
  event.preventDefault();
  const firstName
  const lastName
  const phoneNumber
}

window.addEventListener("load", function(){
  const form = document.getEventById("addressBook");
  form.addEventListener("submit", handleSubmission);
})